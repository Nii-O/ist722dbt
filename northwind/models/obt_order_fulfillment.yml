version: 2

models:
  - name: obt_order_fulfillment
    description: The order fulfilment fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per order does not include product.
